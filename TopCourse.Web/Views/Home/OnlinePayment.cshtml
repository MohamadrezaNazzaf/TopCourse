
@{
    ViewData["Title"] = "نتیجه پرداخت";
}


    <div class="container">
        <nav aria-label="breadcrumb">
            <ul class="breadcrumb">
                <li class="breadcrumb-item"><a href="/">تاپ کورس</a></li>
                <li class="breadcrumb-item active" aria-current="page">نتیجه پرداخت</li>
            </ul>
        </nav>
        </div>
        <div class="container">
            @if (ViewBag.IsSuccess == true)
            {
            <div class="alert alert-success" style="text-align:center">
                <h2>پرداخت با موفقیت انجام شد</h2>
                <!--  <p> کد پیگیری : @ViewBag.Code</p>-->
                <p><a href="/UserPanel">بازگشت به حساب کاربری</a></p>
            </div>
            }
            else
            {
        <div class="alert alert-danger" style="text-align:center">
            <h2>پرداخت با شکست مواجه شد</h2>
            <p><a href="UserPanel">بازگشت به حساب کاربری</a></p>
        </div>
            }
        </div>
